import { LAYOUT } from '@/constants/index';

const root: CustomRouteRecordRaw = {
  path: '/error2',
  name: 'Error2',
  component: LAYOUT,
  meta: {
    title: '错误页面',
    sort: 5,
  },
};

export default root;
